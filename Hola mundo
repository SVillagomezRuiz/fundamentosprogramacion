#include <iostream> //libreria para manejar mensajes cout y cin

using namespace std; //manejo la salida y la entrada standard

int main() // inicio del algoritmo
{
    std::cout<<"Hola Mundo";
    return 0; // No devuelve nada
}
